spring:
  application:
    name: apollo-adminservice
  profiles:
    active: ${apollo_profile}

ctrip:
  appid: 100003172
  
server:
  port: 80
  
logging:
  file: /usr/local/logs/apollo-adminservice.log
